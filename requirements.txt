pydantic>=2.0.0
python-dotenv>=1.0.0
fastapi>=0.100.0
uvicorn>=0.23.0
# LLM enrichment (optional). The agent works without these installed,
# falling back to the pure rule engine.
openai>=1.0.0
freeflow-llm>=0.1.0
google-genai>=2.3.0