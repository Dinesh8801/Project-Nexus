# Optional Declarative Automation Bundle config for the Databricks App.
# Runtime is configured in app.yaml (which takes precedence over any config
# block). GROQ/GEMINI keys are attached as secret-backed environment variables
# in the Databricks Apps UI (or the app's Resources tab).
bundle:
  name: agent1_risk_detection

resources:
  apps:
    agent1_risk_detection:
      name: agent1-risk-detection
      source_code_path: .
      description: Times of Tieto news dashboard + Agent 1 risk-analysis API